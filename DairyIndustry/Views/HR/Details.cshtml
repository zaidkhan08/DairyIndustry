@model DairyIndustry.Models.StaffModel
@{
    Layout = "~/Views/Shared/_HRLayout.cshtml";
    ViewData["Title"] = "Staff Details";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h2 class="fw-bold text-primary mb-0">
            <i class="bi bi-person-lines-fill me-2"></i>Staff Details
        </h2>
        <small class="text-muted">Full profile information</small>
    </div>
    <a asp-action="Index" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left me-1"></i> Back to List
    </a>
</div>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success alert-dismissible fade show">
        <i class="bi bi-check-circle-fill me-2"></i>@TempData["Success"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show">
        <i class="bi bi-exclamation-triangle-fill me-2"></i>@TempData["Error"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="row g-4">

    <!-- LEFT — Profile Card -->
    <div class="col-md-4">
        <div class="card border-0 shadow-sm text-center">
            <div class="card-body py-4">

                @if (!string.IsNullOrEmpty(Model.ProfilePhoto))
                {
                    <img src="@Model.ProfilePhoto"
                         class="rounded-circle border border-3 border-primary mb-3"
                         style="width:130px;height:130px;object-fit:cover;"
                         onerror="this.src='data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%22130%22 height=%22130%22 viewBox=%220 0 130 130%22%3E%3Crect width=%22130%22 height=%22130%22 fill=%22%23e9ecef%22 rx=%2265%22/%3E%3Ccircle cx=%2265%22 cy=%2252%22 r=%2224%22 fill=%22%23adb5bd%22/%3E%3Cellipse cx=%2265%22 cy=%22112%22 rx=%2238%22 ry=%2230%22 fill=%22%23adb5bd%22/%3E%3C/svg%3E'" />
                }
                else
                {
                    <div class="rounded-circle bg-primary bg-opacity-10
                                    d-flex align-items-center justify-content-center mx-auto mb-3"
                         style="width:130px;height:130px;">
                        <i class="bi bi-person-fill text-primary" style="font-size:3.5rem;"></i>
                    </div>
                }

                <h4 class="fw-bold mb-1">@Model.FullName</h4>
                <span class="badge bg-primary mb-2">@Model.RoleName</span>
                <div class="mb-3">
                    <span class="@Model.StatusBadgeClass fs-6">@Model.StatusLabel</span>
                </div>

                @if (Model.Salary.HasValue)
                {
                    <div class="mb-3">
                        <span class="text-muted small">Salary</span>
                        <div class="fw-bold text-success fs-5">₹@Model.Salary.Value.ToString("N0")</div>
                    </div>
                }

                <!-- Update Photo -->
                <form asp-action="UpdatePhoto" method="post" enctype="multipart/form-data"
                      class="border-top pt-3">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="staffId" value="@Model.StaffId" />
                    <label class="form-label small fw-semibold text-muted">Update Photo</label>
                    <input type="file" name="photo" class="form-control form-control-sm mb-2"
                           accept="image/*" />
                    <button type="submit" class="btn btn-outline-primary btn-sm w-100">
                        <i class="bi bi-upload me-1"></i> Upload
                    </button>
                </form>

            </div>

            <!-- Toggle Active -->
            <div class="card-footer bg-white border-top">
                <form asp-action="ToggleActive" method="post">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="id" value="@Model.StaffId" />
                    <input type="hidden" name="currentStatus"
                           value="@Model.IsActive.ToString().ToLower()" />
                    @if (Model.IsActive)
                    {
                        <button type="submit" class="btn btn-outline-danger btn-sm w-100">
                            <i class="bi bi-person-dash-fill me-1"></i> Deactivate Staff
                        </button>
                    }
                    else
                    {
                        <button type="submit" class="btn btn-outline-success btn-sm w-100">
                            <i class="bi bi-person-check-fill me-1"></i> Activate Staff
                        </button>
                    }
                </form>
            </div>
        </div>
    </div>

    <!-- RIGHT — Details -->
    <div class="col-md-8">

        <!-- Personal Info -->
        <div class="card border-0 shadow-sm mb-4">
            <div class="card-header bg-primary text-white fw-semibold">
                <i class="bi bi-person-fill me-2"></i>Personal Information
            </div>
            <div class="card-body">
                <dl class="row mb-0">
                    <dt class="col-sm-4 text-muted">Staff ID</dt>
                    <dd class="col-sm-8">#@Model.StaffId</dd>

                    <dt class="col-sm-4 text-muted">First Name</dt>
                    <dd class="col-sm-8 fw-semibold">@(Model.FirstName ?? "—")</dd>

                    <dt class="col-sm-4 text-muted">Last Name</dt>
                    <dd class="col-sm-8 fw-semibold">@(Model.LastName ?? "—")</dd>

                    <dt class="col-sm-4 text-muted">Role</dt>
                    <dd class="col-sm-8">
                        <span class="badge bg-primary bg-opacity-10 text-primary">
                            @(Model.RoleName ?? "—")
                        </span>
                    </dd>

                    <dt class="col-sm-4 text-muted">Phone</dt>
                    <dd class="col-sm-8">@(Model.Phone ?? "—")</dd>

                    <dt class="col-sm-4 text-muted">Email</dt>
                    <dd class="col-sm-8">@(Model.Email ?? "—")</dd>

                    <dt class="col-sm-4 text-muted">Date of Joining</dt>
                    <dd class="col-sm-8">
                        @(Model.DOJ.HasValue? Model.DOJ.Value.ToString("dd MMM yyyy") : "—")
                    </dd>

                    <dt class="col-sm-4 text-muted">Salary</dt>
                    <dd class="col-sm-8 fw-semibold text-success">
                        @(Model.Salary.HasValue ? "₹" + Model.Salary.Value.ToString("N2") : "—")
                    </dd>

                    <dt class="col-sm-4 text-muted">Status</dt>
                    <dd class="col-sm-8">
                        <span class="@Model.StatusBadgeClass">@Model.StatusLabel</span>
                    </dd>
                </dl>
            </div>
            <div class="card-footer bg-white border-top d-flex gap-2">
                <a asp-action="Edit" asp-route-id="@Model.StaffId" class="btn btn-primary btn-sm">
                    <i class="bi bi-pencil-fill me-1"></i> Edit Details
                </a>
                <a asp-action="Payments" asp-route-staffId="@Model.StaffId"
                   class="btn btn-outline-warning btn-sm">
                    <i class="bi bi-cash-stack me-1"></i> View Payments
                </a>
            </div>
        </div>

        <!-- Assignment -->
        <div class="card border-0 shadow-sm mb-4">
            <div class="card-header bg-primary text-white fw-semibold">
                <i class="bi bi-diagram-3-fill me-2"></i>Assignment
            </div>
            <div class="card-body">
                <dl class="row mb-0">
                    <dt class="col-sm-4 text-muted">Assigned To</dt>
                    <dd class="col-sm-8 fw-semibold">
                        @if (Model.PlantName != null)
                        {
                            <span class="badge bg-info text-dark me-1">Plant</span>
                            @Model.PlantName
                        }
                        else if (Model.CenterName != null)
                        {
                            <span class="badge bg-success me-1">Center</span>
                            @Model.CenterName
                        }
                        else
                        {
                            <span class="text-muted">Not Assigned</span>
                        }
                    </dd>
                </dl>
            </div>
        </div>

        <!-- Bank Details -->
        <div class="card border-0 shadow-sm mb-4">
            <div class="card-header bg-primary text-white fw-semibold">
                <i class="bi bi-bank2 me-2"></i>Bank Account Details
            </div>
            <div class="card-body">
                <dl class="row mb-0">
                    <dt class="col-sm-4 text-muted">Bank Name</dt>
                    <dd class="col-sm-8 fw-semibold">@(Model.BankName ?? "—")</dd>

                    <dt class="col-sm-4 text-muted">Account Number</dt>
                    <dd class="col-sm-8">@(Model.AccountNumber ?? "—")</dd>

                    <dt class="col-sm-4 text-muted">IFSC Code</dt>
                    <dd class="col-sm-8">@(Model.IFSCCode ?? "—")</dd>
                </dl>
            </div>
        </div>

        <!-- ══════════════════════════════════════════════════════════
             FEATURE 1 — LOGIN ACCOUNT STATUS
             Shows whether this staff member has a system login account.
             Pulls from Admin.Users via StaffId FK.
             Read-only — HR can see status but cannot edit credentials
             here. Credential management is via the Admin panel.
        ═══════════════════════════════════════════════════════════ -->
        <div class="card border-0 shadow-sm mb-4"
             style="border-left: 4px solid @(Model.HasLoginAccount ? "#6366f1" : "#e2e8f0") !important;">
            <div class="card-header fw-semibold d-flex align-items-center gap-2"
                 style="background: @(Model.HasLoginAccount ? "#eef2ff" : "#f8fafc");
                        border-bottom: 1px solid @(Model.HasLoginAccount ? "#c7d2fe" : "#e2e8f0");
                        color: @(Model.HasLoginAccount ? "#3730a3" : "#64748b");">
                <i class="bi bi-shield-@(Model.HasLoginAccount ? "lock-fill" : "x") fs-5"
                   style="color: @(Model.HasLoginAccount ? "#6366f1" : "#94a3b8");"></i>
                Login Account
                @if (Model.HasLoginAccount)
                {
                    if (Model.IsLoginActive)
                    {
                        <span class="badge ms-auto bg-success">Active</span>
                    }
                    else
                    {
                        <span class="badge ms-auto bg-secondary">Inactive</span>
                    }
                }
                else
                {
                    <span class="badge ms-auto"
                          style="background:#f1f5f9; color:#64748b;">No Account</span>
                }
            </div>
            <div class="card-body">
                @if (Model.HasLoginAccount)
                {
                    <dl class="row mb-0">
                        <dt class="col-sm-4 text-muted">Username</dt>
                        <dd class="col-sm-8">
                            <span class="fw-semibold">@Model.LoginUsername</span>
                        </dd>

                        <dt class="col-sm-4 text-muted">Account Status</dt>
                        <dd class="col-sm-8">
                            @if (Model.IsLoginActive)
                            {
                                <span class="badge bg-success">
                                    <i class="bi bi-check-circle-fill me-1"></i>Active
                                </span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">
                                    <i class="bi bi-x-circle-fill me-1"></i>Inactive
                                </span>
                            }
                        </dd>

                        @if (Model.LoginCreatedDate.HasValue)
                        {
                            <dt class="col-sm-4 text-muted">Created On</dt>
                            <dd class="col-sm-8 text-muted">
                                @Model.LoginCreatedDate.Value.ToString("dd MMM yyyy")
                            </dd>
                        }
                    </dl>
                    <div class="alert alert-info border-0 py-2 mb-0 mt-3" style="font-size:.8rem;">
                        <i class="bi bi-info-circle-fill me-1"></i>
                        To change username, password or deactivate this account,
                        go to the <strong>Admin Panel → User Management</strong>.
                    </div>
                }
                else
                {
                    <div class="text-center py-3">
                        <i class="bi bi-shield-x text-muted d-block mb-2" style="font-size:2rem; opacity:.3;"></i>
                        <div class="text-muted small">
                            This staff member does not have a system login account.
                        </div>
                        <div class="text-muted small mt-1">
                            To create one, go to
                            <strong>Admin Panel → User Management</strong>
                            or delete and re-register this staff with credentials.
                        </div>
                    </div>
                }
            </div>
        </div>

        <!-- ══════════════════════════════════════════════════════════
             FEATURE 3 — SALARY HISTORY
             Full salary timeline for this staff member.
             Loaded via ViewBag.SalaryHistory from Details action.
             Hidden completely if no history exists yet.
        ═══════════════════════════════════════════════════════════ -->
        @{
            var salaryHistory = ViewBag.SalaryHistory as
            List<DairyIndustry.Models.SalaryHistoryModel>;
        }
        @if (salaryHistory != null && salaryHistory.Any())
        {
            <div class="card border-0 shadow-sm"
                 style="border-left:4px solid #8b5cf6 !important;">
                <div class="card-header fw-semibold d-flex align-items-center gap-2"
                     style="background:#f5f3ff; border-bottom:1px solid #ddd6fe; color:#5b21b6;">
                    <i class="bi bi-clock-history fs-5" style="color:#8b5cf6;"></i>
                    Salary History
                    <span class="badge ms-auto rounded-pill"
                          style="background:#8b5cf6; color:#fff;">
                        @salaryHistory.Count
                    </span>
                </div>
                <div class="card-body p-0">
                    <ul class="list-group list-group-flush">
                        @foreach (var h in salaryHistory)
                        {
                            <li class="list-group-item px-3 py-3">
                                <div class="d-flex align-items-start gap-3">
                                    <div class="mt-1">
                                        <i class="bi @h.ChangeIcon fs-5
                                                   @(h.ChangeDirection == "increase" ? "text-success" :
                                                                                        h.ChangeDirection == "decrease" ? "text-danger" :
                                                                                        "text-primary")"></i>
                            </div>
                            <div class="flex-grow-1">
                                <div class="d-flex align-items-center gap-2 mb-1">
                                    <span class="fw-bold fs-6">
                                        ₹@h.NewSalary.ToString("N2")
                                    </span>
                                    @if (h.OldSalary.HasValue)
                                            {
                                                <span class="text-muted small">
                                                    from ₹@h.OldSalary.Value.ToString("N2")
                                                </span>
                                                <span class="badge @h.ChangeBadgeClass rounded-pill"
                                                      style="font-size:.65rem;">
                                                    @(h.ChangeDirection == "increase" ? "+" : "")
                                                    ₹@(Math.Abs(h.NewSalary - h.OldSalary.Value).ToString("N2"))
                                                </span>
                                            }
                                            else
                                            {
                                                <span class="badge bg-primary rounded-pill"
                                                      style="font-size:.65rem;">
                                                    Starting Salary
                                                </span>
                                            }
                                        </div>
                                        @if (!string.IsNullOrEmpty(h.Reason))
                                        {
                                            <div class="text-muted small mb-1">
                                                <i class="bi bi-chat-left-text me-1"></i>@h.Reason
                                            </div>
                                        }
                                        <div class="text-muted small">
                                            <i class="bi bi-calendar3 me-1"></i>
                                            @h.ChangedDate.ToString("dd MMM yyyy, hh:mm tt")
                                            @if (!string.IsNullOrEmpty(h.ChangedBy))
                                            {
                                                <span class="ms-2">
                                                    <i class="bi bi-person-fill me-1"></i>@h.ChangedBy
                                                </span>
                                            }
                                        </div>
                                    </div>
                                </div>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        }

    </div>
</div>
