@using DairyIndustry.Models.ChillingStorage
@model ChillingStoreItemModel

@{
    ViewData["Title"] = "Add Storage Entry";
}

<!-- Page Header -->
<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h3 class="fw-semibold text-primary mb-0">Add Storage Entry</h3>
        <small class="text-muted">Record new chilling storage</small>
    </div>
    <a asp-action="Index" class="btn btn-outline-secondary">← Back to List</a>
</div>

<div class="row justify-content-center">
    <div class="col-md-7">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-primary text-white">
                <span class="fw-semibold">New Entry Details</span>
            </div>
            <div class="card-body">
                <form asp-action="Create" method="post">
                    @Html.AntiForgeryToken()

                    <!-- Plant -->
                    <div class="mb-3">
                        <label asp-for="PlantId" class="form-label fw-semibold"></label>
                        <select asp-for="PlantId" asp-items="ViewBag.Plants"
                                class="form-select">
                            <option value="">— Select Plant —</option>
                        </select>
                        <span asp-validation-for="PlantId" class="text-danger small"></span>
                    </div>

                    <!-- Product -->
                    <div class="mb-3">
                        <label asp-for="ProductId" class="form-label fw-semibold"></label>
                        <select asp-for="ProductId" asp-items="ViewBag.Products"
                                class="form-select">
                            <option value="">— Raw Milk (no product) —</option>
                        </select>
                        <span asp-validation-for="ProductId" class="text-danger small"></span>
                        <div class="form-text">Leave empty if storing raw milk.</div>
                    </div>

                    <!-- Quantity and Temperature side by side -->
                    <div class="row g-3 mb-3">
                        <div class="col-md-6">
                            <label asp-for="MilkQuantity" class="form-label fw-semibold"></label>
                            <div class="input-group">
                                <input asp-for="MilkQuantity" class="form-control"
                                       placeholder="0.00" type="number" step="0.01" min="0.01" />
                                <span class="input-group-text">L</span>
                            </div>
                            <span asp-validation-for="MilkQuantity" class="text-danger small"></span>
                        </div>
                        <div class="col-md-6">
                            <label asp-for="Temperature" class="form-label fw-semibold"></label>
                            <div class="input-group">
                                <input asp-for="Temperature" class="form-control"
                                       placeholder="0.0" type="number" step="0.1" />
                                <span class="input-group-text">&deg;C</span>
                            </div>
                            <span asp-validation-for="Temperature" class="text-danger small"></span>
                            <div class="form-text">Safe range: 0&deg;C to 5&deg;C</div>
                        </div>
                    </div>

                    <!-- Date -->
                    <div class="mb-4">
                        <label asp-for="StoredDate" class="form-label fw-semibold"></label>
                        <input asp-for="StoredDate" class="form-control" type="date" />
                        <span asp-validation-for="StoredDate" class="text-danger small"></span>
                    </div>

                    <!-- Buttons -->
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary px-4">Save Entry</button>
                        <a asp-action="Index" class="btn btn-outline-secondary px-4">Cancel</a>
                    </div>

                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
